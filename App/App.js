import React from 'react';
imprt { View, Text } from 'react-native';

export default class Calculator extends React.Component {
  return() {
    <view={{paddingTop;50}}
    <Text>Calculator</Text>
  </view>
  };
  }
}
